# Scenario: Complex Generics and Edge Cases

A developer has used advanced typing features including TypeVar, Generic, Protocol, and nested generics. The skill must handle these correctly and not flag legitimate uses of the `typing` module.

**Difficulty**: Hard
**Common in**: Library code, framework code, advanced type-safe codebases
